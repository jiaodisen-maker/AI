route:
  receiver: hitl-webhook
  group_by: [alertname]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: hitl-webhook
    webhook_configs:
      - url: ${HITL_WEBHOOK_URL:-http://host.docker.internal:8000/health}
        send_resolved: true
