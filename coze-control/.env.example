# 复制为 .env（已在 .gitignore），只在你本地存在。

COZE_API_BASE=https://api.coze.cn
COZE_PAT=pat_REPLACE_ME_WITH_NEW_ROTATED_TOKEN
COZE_WORKSPACE_ID=                 # 空串=全部；填具体 ID 则只处理这一个

# 可选：评测/优化用的 LLM（比如本地 Claude / 火山的 doubao）
JUDGE_API_BASE=
JUDGE_API_KEY=
JUDGE_MODEL=doubao-pro
